{
  "name": "@readdo/eval-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "lint": "tsc -p tsconfig.json --noEmit",
    "eval": "tsx src/index.ts"
  },
  "dependencies": {
    "@readdo/contracts": "workspace:*",
    "@readdo/core": "workspace:*",
    "@readdo/shared": "workspace:*"
  }
}
