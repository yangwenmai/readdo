{
  "name": "readdo",
  "version": "0.1.0",
  "private": true,
  "description": "Read→Do monorepo",
  "packageManager": "pnpm@10.5.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "dev:api": "pnpm --filter @readdo/api dev",
    "dev:web": "pnpm --filter @readdo/web dev",
    "dev:extension": "pnpm --filter @readdo/extension dev",
    "eval": "pnpm --filter @readdo/eval-runner eval"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
